/**\n * CMMS Inventory Manager - Supabase Integration Code\n * \n * Copy these code snippets into: frontend/src/components/CMSSModule.jsx\n * In the InventoryManager component function\n */\n\n// ============================================================\n// STEP 1: ADD IMPORTS AT TOP OF FILE\n// ============================================================\n\n// Add this line with other imports (around line 30):\nimport { \n  getDepartmentInventory, \n  addInventoryItem, \n  deleteInventoryItem,\n  updateInventoryQuantity,\n  getLowStockItems\n} from '../services/cmmsInventoryService';\n\n// ============================================================\n// STEP 2: ADD STATE FOR LOADING IN InventoryManager\n// ============================================================\n\n// Add this inside InventoryManager function (after expandedItems state):\nconst [isLoading, setIsLoading] = useState(false);\nconst [departmentId, setDepartmentId] = useState(null);\nconst [companyId, setCompanyId] = useState(null);\n\n// ============================================================\n// STEP 3: LOAD INVENTORY FROM SUPABASE ON MOUNT\n// ============================================================\n\n// Add this useEffect (after expandedItems state is defined):\nuseEffect(() => {\n  // Get department and company from user or current context\n  // This assumes you have access to user's department and company info\n  const loadInventoryData = async () => {\n    if (!departmentId || !companyId) {\n      console.log('⏳ Waiting for department/company info...');\n      return;\n    }\n    \n    setIsLoading(true);\n    try {\n      const result = await getDepartmentInventory(departmentId);\n      \n      if (result.success && result.data?.length > 0) {\n        // Transform database format to component format\n        const transformedData = result.data.map(item => ({\n          id: item.id,\n          name: item.item_name,\n          category: item.category,\n          quantity: item.quantity_in_stock,\n          minStock: item.reorder_level,\n          cost: item.unit_price,\n          storeman: item.supplier_name,\n          code: item.item_code,\n          description: item.description,\n          createdAt: item.created_at,\n          lastRestocked: item.last_stock_check,\n          location: item.storage_location,\n          binNumber: item.bin_number\n        }));\n        \n        setCmmsData(prev => ({\n          ...prev,\n          inventory: transformedData\n        }));\n        \n        console.log('✅ Loaded', transformedData.length, 'inventory items from Supabase');\n      } else {\n        console.log('ℹ️ No inventory items found for this department');\n        setCmmsData(prev => ({\n          ...prev,\n          inventory: []\n        }));\n      }\n    } catch (error) {\n      console.error('❌ Error loading inventory:', error);\n      alert('Error loading inventory: ' + error.message);\n    } finally {\n      setIsLoading(false);\n    }\n  };\n  \n  loadInventoryData();\n}, [departmentId, companyId]);\n\n// ============================================================\n// STEP 4: UPDATE handleAddItem TO SAVE TO SUPABASE\n// ============================================================\n\n// Replace the existing handleAddItem function with this:\nconst handleAddItem = async () => {\n  if (!canEditInventory) {\n    alert('🔒 You do not have permission to add inventory items.');\n    return;\n  }\n  \n  if (!newItem.name) {\n    alert('⚠️ Please enter an item name');\n    return;\n  }\n  \n  if (!departmentId || !companyId) {\n    alert('⚠️ Department/Company information is missing');\n    return;\n  }\n  \n  try {\n    setIsLoading(true);\n    \n    const result = await addInventoryItem(departmentId, companyId, newItem);\n    \n    if (result.success) {\n      // Add to local state immediately for instant feedback\n      const newItemWithId = {\n        id: result.data.id,\n        name: result.data.item_name,\n        category: result.data.category,\n        quantity: result.data.quantity_in_stock,\n        minStock: result.data.reorder_level,\n        cost: result.data.unit_price,\n        storeman: result.data.supplier_name,\n        code: result.data.item_code,\n        createdAt: result.data.created_at\n      };\n      \n      setCmmsData(prev => ({\n        ...prev,\n        inventory: [...prev.inventory, newItemWithId]\n      }));\n      \n      // Clear form\n      setNewItem({ \n        name: '', \n        category: 'Spare Parts', \n        quantity: 0, \n        minStock: 0, \n        cost: 0, \n        storeman: '' \n      });\n      \n      console.log('✅ Item saved to Supabase:', result.data.id);\n      alert('✅ Inventory item saved successfully!');\n    } else {\n      console.error('❌ Error saving item:', result.error);\n      alert('❌ Error saving item: ' + result.error);\n    }\n  } catch (error) {\n    console.error('❌ Unexpected error:', error);\n    alert('❌ Unexpected error: ' + error.message);\n  } finally {\n    setIsLoading(false);\n  }\n};\n\n// ============================================================\n// STEP 5: UPDATE handleDeleteItem TO DELETE FROM SUPABASE\n// ============================================================\n\n// Replace the existing handleDeleteItem function with this:\nconst handleDeleteItem = async (itemId) => {\n  if (!canEditInventory) {\n    alert('🔒 You do not have permission to delete inventory items.');\n    return;\n  }\n  \n  if (!window.confirm('⚠️ Are you sure you want to delete this inventory item? This action cannot be undone.')) {\n    return;\n  }\n  \n  try {\n    setIsLoading(true);\n    \n    const result = await deleteInventoryItem(itemId);\n    \n    if (result.success) {\n      // Remove from local state\n      setCmmsData(prev => ({\n        ...prev,\n        inventory: prev.inventory.filter(item => item.id !== itemId)\n      }));\n      \n      // Close expanded view if open\n      setExpandedItems(prev => {\n        const updated = { ...prev };\n        delete updated[itemId];\n        return updated;\n      });\n      \n      console.log('✅ Item deleted from Supabase:', itemId);\n      alert('✅ Item deleted successfully!');\n    } else {\n      console.error('❌ Error deleting item:', result.error);\n      alert('❌ Error deleting item: ' + result.error);\n    }\n  } catch (error) {\n    console.error('❌ Unexpected error:', error);\n    alert('❌ Unexpected error: ' + error.message);\n  } finally {\n    setIsLoading(false);\n  }\n};\n\n// ============================================================\n// STEP 6: SET DEPARTMENT & COMPANY ID FROM USER CONTEXT\n// ============================================================\n\n// Add this useEffect early in the component (after other useEffect hooks):\nuseEffect(() => {\n  // Get the user's department and company\n  // This depends on how your user info is stored\n  // Example below - adjust to match your app's structure:\n  \n  // Option 1: If user has department/company in their profile\n  if (user?.departmentId && user?.companyId) {\n    setDepartmentId(user.departmentId);\n    setCompanyId(user.companyId);\n  }\n  // Option 2: If stored in cmmsData\n  else if (cmmsData?.departmentId && cmmsData?.companyId) {\n    setDepartmentId(cmmsData.departmentId);\n    setCompanyId(cmmsData.companyId);\n  }\n  // Option 3: From component props\n  else if (departmentId && companyId) {\n    // Already set\n  }\n}, [user, cmmsData]);\n\n// ============================================================\n// STEP 7: ADD LOADING STATE TO UI\n// ============================================================\n\n// Add this to the inventory section (around line 2160 before the form):\n{isLoading && (\n  <div className=\"glass-card p-4 md:p-6 bg-blue-500 bg-opacity-10 border-l-4 border-blue-500 flex items-center gap-3\">\n    <Loader className=\"w-5 h-5 animate-spin text-blue-400\" />\n    <p className=\"text-blue-300 text-sm md:text-base\">Loading inventory data...</p>\n  </div>\n)}\n\n// ============================================================\n// STEP 8: TEST THE INTEGRATION\n// ============================================================\n\n/*\nTesting Checklist:\n\n1. LOGIN AS STOREMAN\n   [ ] Can see inventory tab\n   [ ] Can see \"Add Inventory Item\" form\n   [ ] Can add new item\n   [ ] Item appears in list immediately\n   [ ] Item shows in collapsed view\n   [ ] Can click to expand and see details\n   [ ] Can delete items\n   [ ] Page refresh → items still there\n\n2. LOGIN AS TECHNICIAN\n   [ ] Can see inventory tab\n   [ ] Can see inventory items\n   [ ] Cannot see \"Add Inventory Item\" form\n   [ ] Shows \"View-Only Mode\" badge\n   [ ] Can expand items to see details\n   [ ] Cannot edit or delete items\n\n3. CHECK SUPABASE\n   [ ] Go to Supabase dashboard\n   [ ] Check cmms_inventory_items table\n   [ ] Verify items are saved with correct data\n   [ ] Check timestamps (created_at, updated_at)\n\n4. PERMISSION TESTING\n   [ ] Admin: Can add/edit/delete\n   [ ] Coordinator: Can view only\n   [ ] Supervisor: Can view only\n   [ ] Technician: Can view only\n   [ ] Finance: Can view only\n   [ ] Storeman: Can add/edit/delete\n   [ ] Guest: Can view only (if department assigned)\n\n5. DATA VALIDATION\n   [ ] Empty form shows error\n   [ ] Large numbers format correctly (UGX 1,000,000)\n   [ ] Dates display correctly\n   [ ] Low stock items show orange badge\n   [ ] Expand/collapse works smoothly\n*/\n\n// ============================================================\n// STEP 9: TROUBLESHOOTING\n// ============================================================\n\n/*\nIF ITEMS DON'T LOAD:\n1. Check browser console for errors (F12 → Console tab)\n2. Verify departmentId and companyId are set:\n   console.log('Dept:', departmentId, 'Company:', companyId)\n3. Check Supabase connection:\n   - Go to Supabase dashboard\n   - Check table has data\n   - Verify RLS policies allow your user\n\nIF ITEMS DON'T SAVE:\n1. Check browser console for errors\n2. Verify Supabase write permission:\n   - Check RLS policy on cmms_inventory_items\n   - Ensure user role is 'storeman' or 'admin'\n3. Check SQL error:\n   - Open Supabase SQL editor\n   - Run: SELECT * FROM cmms_inventory_items LIMIT 1;\n   - Verify table structure\n\nIF ITEMS DISAPPEAR AFTER DELETE:\n1. This is correct behavior - soft delete sets is_active=false\n2. Items are hidden but data preserved\n3. To see deleted items, check with is_active=true condition:\n   - SELECT * FROM cmms_inventory_items \n   - WHERE department_id=XXX AND is_active=false;\n*/\n"